# v1.14.0 release and installed validation

Engineering Board 1.14.0 is published and verified. F005 and B009 are resolved.

## Identity and gates

- [PR175](https://github.com/GhostlyGawd/engineering-board/pull/175) merged as
  `eb275b68369b04bbbc944a584f869fc07dcf05b3`.
- Annotated `v1.14.0` dereferences to that exact commit. The merged tree matches
  reviewed head `c50d814`.
- Both latest PR CI runs
  [34378552368](https://github.com/GhostlyGawd/engineering-board/actions/runs/34378552368)
  and [34378559126](https://github.com/GhostlyGawd/engineering-board/actions/runs/34378559126)
  passed. [Main CI 34378838422](https://github.com/GhostlyGawd/engineering-board/actions/runs/34378838422)
  passed before publication. All 22 suites pass.
- `claude plugin validate --strict .` passed without warnings on the prepared
  and refreshed release trees.
- [Release workflow 34379065314](https://github.com/GhostlyGawd/engineering-board/actions/runs/34379065314)
  completed successfully, including the explicit MCP Registry publication step
  and PyPI job. The workflow owned the tag, release and registry writes.

## Delivered behavior

`board_get_entry` now opens a canonical H id returned by `board_context`, using
the same root and project. Full Markdown exposes claim, status, provenance,
alternatives, falsifier and outcome history. It returns the validated snapshot
without confirmation, outcome changes or repository writes. Ordinary B/F/Q/O/L
read/list/update contracts are unchanged. Malformed or linked hypothesis
registry data fails closed; the reader does not repair it.

The release also delivers the previously queued D.1 v2 contract/reporting
corrections and deleted-working-directory claim fallback. Historical D.1
product-effect gates, locked corpora and observed outcomes retain their meaning.

## Publication verification

| Surface | Observed result |
|---|---|
| GitHub Release | Published v1.14.0 at 2026-09-09T16:49:02Z; not draft or prerelease |
| MCP bundle | Downloaded 68,557 bytes; SHA-256 matches the prepared pin and registry |
| MCP Registry | Active/latest 1.14.0; published 2026-09-09T16:49:04.140006Z; same asset URL and digest |
| PyPI | Wheel and sdist 1.14.0 present, unyanked; both downloaded and their hashes verified |
| Product site | HTTPS 200; 33,350 bytes; product and 19-tool markers present; no visual redesign claimed |

Bundle SHA-256:
`0b259fac619543308043f804dd5d5b49612cf018fcf61209e72f3f752d66cc67`.

PyPI wheel SHA-256:
`6b12d9efa9590c2badbd29fb32d61730638d378eda49e8b6ca9e20db625d9b2f`;
68,714 bytes. Source distribution SHA-256:
`6037631eae62dcb9b023014a0720a2dcbbb9b4b067dd2f652d5de86f55cfa8fd`;
74,530 bytes.

Exact metadata and installed-file hashes are retained in
[publication-verification.json](f005-hypothesis-details-2026-09-09/publication-verification.json).

## Installed workflow

- Codex marketplace upgraded and `engineering-board@engineering-board`
  installed at 1.14.0. Its cache HEAD is the exact release commit; pinned
  marketplace/host-boundary validation exposes 19 tools and passes.
- Claude marketplace updated and the user-scoped plugin upgraded 1.13.5→1.14.0.
  Strict validation of its installed marketplace passes without warnings.
  Its copied cache has no Git metadata; seven runtime/manifest/launcher files
  were compared byte-for-byte against the tagged source instead.
- Both host caches match the tagged source for those seven files.
- A fresh Python 3.9 environment installed 1.14.0 from public PyPI. Both package
  metadata and MCP handshake report 1.14.0; installed MCP/core module bytes match
  the tagged source.
- Fresh processes from Codex, Claude and public PyPI each retrieve H101/H102,
  open exact full details, preserve proposed status, reject missing H ids,
  and leave repository bytes, paths, links and permissions unchanged. Each
  retained probe makes zero model calls.

Receipts:
[Codex](f005-hypothesis-details-2026-09-09/installed-codex-1.14.0.json),
[Claude](f005-hypothesis-details-2026-09-09/installed-claude-1.14.0.json),
[PyPI](f005-hypothesis-details-2026-09-09/installed-pypi-1.14.0.json).
Existing app sessions can retain their old MCP process; start a new session to
load the updated plugin. These receipts verify fresh installed processes, not
hot replacement of the current conversation's process.

## Review findings retained, not waived

[Source review](f005-hypothesis-details-2026-09-09/SOURCE-REVIEW.md) records
independent F005 validation and the strengthened installation probe. The initial
file-only probe could miss empty runtime directories; corrected inventories
cover paths, link identities and permissions as well as bytes.

Unpacked-bundle verification then found B009: handshake version 0.0.0 despite
release metadata 1.14.0. Publication stopped. The corrected source/bundle/owning
distribution fallback was independently reviewed, including a Python 3.9
symlink-resolution failure. Runtime checks pass on all seven available versions,
Python 3.8.20 through 3.14.7. Full MCP tests pass 382 checks on Python 3.12.13.

The first Linux CI runs 34375964139/34375971827 failed a test-oracle assumption:
Python 3.12 filters missing RECORD paths before resolution, so a cyclic row can
be ignored and the valid owning version returned. Test-only correction
`a4bfc8c` permits exactly the owned version or conservative unknown for that
real-cycle fixture. Forced resolution failure still requires exactly 0.0.0;
ownership and precedence checks remain exact. Independent review passed at
`c50d814`, and both corrected CI runs passed before merge. No production change
or checksum change was needed for this test correction.

The initial unpublished bundle pin `d506f134…` is superseded by the coordinated
refresh above; no published tag or asset was overwritten. Failure receipts
remain alongside successful ones. Setuptools's pre-existing license-table and
classifier deprecation warnings are recorded in O003; wheel builds succeeded.
They were not strict Claude validation warnings.

## Outcome boundary and timing

This release proves full hypothesis evidence is reachable through the supported
read tool and verifies package/install identity. It does not prove improved
diagnoses, saved time, willingness to pay or a D.1 pass. The prior workflow pilot
and direct Learning-use ledger remain separate evidence, with no backfilled
production outcomes or success counters. The team structure is unchanged.

F005 claim owner-file creation: 2026-09-09T15:48:59Z. Installed checks and host
identity comparison completed by 16:53:44Z: about 64m45s elapsed, including review,
packaging, CI and publication. This is not isolated implementation effort or
product time savings. Agent monetary cost was not measured. Retention owner:
Rhen McLeod; source and receipts are retained in Git, and immutable publication
assets remain at the linked release/registries. O003 is maintenance follow-up;
no actionable bug or feature remains open on this board at closeout.

Independent closeout evidence review passed at `d85294d`, checking public
publication identities, installed source hashes and the three runtime receipts.
Its minor stale Codex-update status sentence in F005 was corrected; no product
or evidentiary behavior changed.
