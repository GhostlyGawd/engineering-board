# v1.13.1 release and installed-plugin closeout — 2026-08-15

## Scope

This record closes the publication and installed-plugin proof that remained
open after the B070 repository fix. It separates released evidence from the
earlier repository-side alignment record.

## Release evidence

| Surface | Observation | Status |
|---|---|---|
| Merged source | PR #148 merged to `main` as `d5bb2b8eddd8a6f9ca5d5c10453278499b9d92c3`. Both PR test runs and the exact merged-main test run passed. | Passed |
| Immutable tag | Annotated tag `v1.13.1` resolves to merged commit `d5bb2b8eddd8a6f9ca5d5c10453278499b9d92c3`. | Passed |
| GitHub Release | Release workflow run `31858829953` published a non-draft, non-prerelease `v1.13.1` release and attached `engineering-board-mcp.mcpb`. | Passed |
| Bundle integrity | The downloaded release asset produced SHA-256 `5b10522f913de8af80d805cbb3181609d234d2817ba7e63bc29296e68f80d42d`, equal to the pin in `mcp-server/server.json`. | Passed |
| PyPI | The live PyPI JSON endpoint returned version `1.13.1`, two non-yanked artifacts, and no publication error. | Passed |
| MCP Registry | The official `v0.1` Registry search returned the Engineering Board server at version `1.13.1`. | Passed |

## Installed-plugin evidence

The WSL marketplace refresh switched discovery from the legacy Claude catalog
to `.agents/plugins/marketplace.json`. A supported remove-and-add sequence then
installed version `1.13.1` at the plugin cache path. The installed Git checkout
has HEAD `d5bb2b8eddd8a6f9ca5d5c10453278499b9d92c3` and an exact tag description of
`v1.13.1`.

A fresh Codex 0.145.0 process loaded all nine Engineering Board skills. Its
`/mcp` status listed the `engineering-board` server and all 19 tools. An initial
agent self-report of zero tools was disproved by the client status surface and
successful tool calls.

## Retrieval and Learning evidence

The released `board_context` tool returned L003 and L005 for
`mcp-server/engineering_board_mcp.py`, each at score 40. An exact repeat kept
result order `L003, L005`, context fingerprint `ctx-d1d7b35fc0b66014`, and
source fingerprint
`71dc00c287be0ce3f97162663387a9bd707a5ace84e39bf1f43c5cb220e613e5`.
An unrelated `brand/tokens.css` query returned no results. Three B070 queries
also returned no results as designed because an isolated bug is a structural
signal, while eligible results are clusters, hypotheses, Learnings, or negative
memory.

The F003 moment-of-need summary returned L003 and L005 for real entry B069 and
returned no summary for the unrelated F003 control.

## Residual environment evidence

The Windows catalog also refreshed to the Codex-native marketplace at merged
commit `d5bb2b8`. Its supported removal could not delete the active 1.13.1 cache
directory because Codex worker processes held it open. The failed removal left
that directory empty, and the supported add command then failed its backup step
with access denied. No worker was terminated because that could interrupt other
active Codex threads. Windows reinstall remains an environment repair item; it
does not invalidate the successful fresh WSL tag-provenance proof.

## New dogfood findings

The released session also captured and promoted B071 through B073 for Codex
project-root resolution, unsupported prompt Stop hooks, and SessionStart open
count inflation from convention examples. These findings remain open and are
not part of the B070 fix scope.

## Closeout validation

The final closeout branch passed all 21 maintained suites. `git diff --check`
also passed. A full graph rebuild produced source fingerprint
`cf5a1f67ed439fcbdf522f830ee902d1aa3c4dccb63079b357ca1b9249d2509c`,
with three open nodes (B071 through B073) in one dense hook cluster. B070 is the
newest archive row and is absent from open graph state.

## Alignment review

This closeout changes dated evidence and board lifecycle state only. The
repository-side marketplace contract, release instructions, architecture,
README install guidance, changelog, tests, security and privacy boundaries, MCP
semantics, and visual behavior remain as reviewed in
`2026-08-15-b070-codex-marketplace-release-pin.md`. No normative surface needs a
second change for this observation-only closeout.

## Conclusion

B070 has complete released evidence: a fresh Codex install resolved the plugin
from the immutable `v1.13.1` tag, and the installed commit equals the published
tag target. B070 can resolve. The Windows cache lock and B071 through B073 remain
explicitly separate follow-up work.
