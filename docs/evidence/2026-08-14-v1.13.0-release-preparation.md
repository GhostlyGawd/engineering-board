# v1.13.0 release preparation — 2026-08-14

## Outcome

Engineering Board version 1.13.0 is prepared from `main` commit
`7d7ea1f5150adae8b909bfa82c4d6372e55e7f6a`.

The preparation script moved the documented Unreleased changes into the
version 1.13.0 changelog section. It updated all product-version mirrors, the
release asset URL, and the reproducible MCP bundle checksum. Publication is
not yet complete. The separate release-validation record will contain the
merged release commit and external publication evidence.

The prepared bundle boundary is:

```text
version: 1.13.0
bundle: engineering-board-mcp.mcpb
bundle_sha256: b3520ab7d953aa079784f1bbdb488e5df3db62815061a85f23ed532f8b5d955b
```

## Documentation alignment workpad

| Requirement or change | Implementation path | Verification | Documentation path | Drift classification and status |
|---|---|---|---|---|
| Publish the compatible Unreleased features as version 1.13.0. | `.claude-plugin/plugin.json` is authoritative. The Codex manifest, marketplace entry, MCP manifests, PyPI metadata, and README badge use the same version. | `scripts/prepare-release.py 1.13.0`; `tests/version-coherence.sh`; `tests/release-preparation.sh`; full suite before merge. | `CHANGELOG.md`, `README.md`, and `docs/RELEASING.md`. | Version drift repaired in the prepared tree. External surfaces remain pending until publication. |
| Publish the exact reproducible MCP bundle. | `mcp-server/server.json` pins the versioned release URL and SHA-256 value. `mcp-server/test_mcp_server.py` reconstructs the same MCP-only archive as `mcp-server/build-mcpb.sh`. | Independent local rebuild, in-process archive reconstruction, and release-workflow checksum gate. | `CHANGELOG.md`, `docs/RELEASING.md`, and the future `docs/evidence/2026-08-14-v1.13.0-release-validation.md`. | Bundle drift and the stale test reconstruction are repaired locally. Published asset evidence is pending. |
| Keep GitHub, PyPI, and the official MCP Registry aligned. | `.github/workflows/release.yml` owns the tag, GitHub Release, asset, MCP Registry publication, and PyPI publication. | Exact-main-SHA workflow dispatch and post-publication queries. | `docs/RELEASING.md` and the future release-validation record. | External publication is intentionally open until the workflow completes. |
| Keep user-facing release claims accurate. | The changelog gets the dated 1.13.0 section and the README badge gets 1.13.0. The product site follows merged `main`. | Changelog extraction test, merged-main CI, Pages result, and HTTP content check. | `CHANGELOG.md`, `README.md`, and product-site content. | Source documentation is prepared. Live site evidence is pending. |
| Preserve unrelated product contracts. | Release preparation does not change MCP protocol dates, context contract version 3, ranking rule version 2, worker schemas, canonical Markdown schemas, storage, security boundaries, or tool grants. | Full regression suite and semantic diff review. | `ARCHITECTURE.md`, `SECURITY.md`, `docs/PRODUCT_EVOLUTION_SPEC.md`, setup guidance, and prior dated evidence. | Reviewed and unaffected. |

## Evidence boundary

This file records local release preparation. It does not claim that version
1.13.0 is tagged, published, installed, or active. The post-publication
closeout must add a separate dated validation file and preserve this record.
