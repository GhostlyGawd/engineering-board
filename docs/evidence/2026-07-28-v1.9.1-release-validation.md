# v1.9.1 release validation — 2026-07-28

## Outcome

Engineering Board Milestone B shipped as v1.9.1 on 2026-07-28. The release
publishes the repository-owned stable-pattern and deterministic graph pipeline
validated in the Milestone B implementation report.

The release boundary is:

```text
release: v1.9.1
commit: e93276a989a9c98ad2002e1cff1cf7bceecaaecd
bundle: engineering-board-mcp.mcpb
bundle_size: 121813 bytes
bundle_sha256: f6b08bbecc60033e2ecdc04833800e0451f00434dfd1f4870efaa20d866bc819
```

## Merge and release sequence

1. [PR #97](https://github.com/GhostlyGawd/engineering-board/pull/97)
   merged the Milestone B implementation as
   `ea34a0c5237bc627c0a5bb86bca262b65e07ccbd`.
2. The v1.9.0 workflow created an annotated tag, then failed closed when the
   bundle-content parity test omitted the shared core module. It did not
   publish a GitHub Release, PyPI distribution, or MCP Registry version.
3. [PR #98](https://github.com/GhostlyGawd/engineering-board/pull/98)
   corrected the parity test and coordinated release metadata. It merged as
   `e93276a989a9c98ad2002e1cff1cf7bceecaaecd`.
4. The annotated v1.9.1 tag dereferences to that merge commit.
5. The v1.9.1 release workflow completed both the release and PyPI jobs.

The immutable v1.9.0 tag remains historical evidence of the failed-closed
attempt. v1.9.1 is the first externally published Milestone B version.

## External publication evidence

Observed on 2026-07-28:

| Surface | Observation | Result |
|---|---|---|
| GitHub Release | [v1.9.1](https://github.com/GhostlyGawd/engineering-board/releases/tag/v1.9.1) is the latest release and targets the v1.9.1 merge commit. | Published |
| Release asset | GitHub reports `engineering-board-mcp.mcpb`, 121813 bytes, with the pinned SHA-256 digest above. | Digest aligned |
| Release workflow | [Run 30333867505](https://github.com/GhostlyGawd/engineering-board/actions/runs/30333867505) completed successfully. Tag/version validation, reproducible bundle verification, GitHub Release publication, official MCP Registry publication, package build, and PyPI trusted publishing passed. | Passed |
| PyPI | [`engineering-board-mcp` 1.9.1](https://pypi.org/project/engineering-board-mcp/1.9.1/) exposes a wheel and source distribution and is not yanked. | Published |
| Official MCP Registry | `io.github.GhostlyGawd/engineering-board` version 1.9.1 is active and latest. Its MCPB package URL and `fileSha256` match the GitHub release asset. | Published and aligned |
| Product site | [GitHub Pages](https://ghostlygawd.github.io/engineering-board/) returned HTTP 200 with the Milestone B pattern-intelligence content. Pages reports status `built` from `gh-pages` at `/`. | Deployed |
| Pages workflow | [Run 30333565020](https://github.com/GhostlyGawd/engineering-board/actions/runs/30333565020) succeeded for the PR #97 merge. PR #98 changed packaging and release metadata only, so it did not require a new product-site build. | Passed; later change unaffected |

## Validation carried into the release

The tagged implementation had these local release-tree results before merge:

```text
bash tests/run-all.sh
RUN-ALL SUMMARY: 16 pass, 0 fail (of 16 suites)

bash mcp-server/run-tests.sh
RESULT: PASS (163 checks)

bash tests/orchestration/milestone-b-pattern-pipeline.sh
milestone-b-pattern-pipeline: 15 checks passed
```

Both implementation PRs also passed their two required `run-all` GitHub
Actions checks before merge. The release workflow then rebuilt the MCP bundle
and verified the pinned digest from the tagged tree before publication.

## Documentation alignment

| Drift class | Disposition |
|---|---|
| Behavior and specification | Repaired in the implementation. This closeout marks the accepted Milestone B contract as shipped without adding Milestone C behavior. |
| Commands and setup | Repaired. `/board-promote`, `/board-pattern`, and shared `/board-graph` guidance match the shipped adapters and authority boundaries. |
| Schema and architecture | Repaired. Stable P### identity, `pattern_ids`, alias resolution, graph provenance, and disposable cache authority match the shipped core. |
| MCP and packaging | Repaired. Fifteen tools, the two-module Python package, MCPB contents, manifests, PyPI version, registry record, and release digest align at v1.9.1. |
| Security and privacy | Repaired. Preview/apply authority, linked-input refusal, concurrent-source checks, partial-write receipts, and local cache boundaries cover the new paths. |
| Landing, LLM index, and visual | Repaired in PR #97 and deployed. The site describes the deterministic pattern pipeline and uses sanitized fixture evidence. |
| Historical evidence | Preserved. The v1.8.0 reports and the pre-publication Milestone B report remain dated observations. This report supersedes only their later publication state. |
| PM/Worker, claims, view, demo, and hypothesis surfaces | Reviewed and unaffected, except for the documented PM delegation to the shared promotion core. Existing compatibility suites remained green. |

Path, keyword, version, and count checks prevent silent omission. They do not
prove semantic correctness. The classifications above record the required
implementation and reviewer judgment.

## Product boundary after release

v1.9.1 delivers reliable evidence promotion, stable pattern identity, and
explainable graph memory. It does not generate or confirm root-cause
hypotheses, require SQLite, aggregate across repositories, or add a hosted
service. Those boundaries remain governed by the central product spec.
