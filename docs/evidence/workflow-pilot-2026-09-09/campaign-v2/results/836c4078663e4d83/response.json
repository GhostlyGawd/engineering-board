{"changes":"Updated `ingest_live` in `readings.py` to convert text directly to `float`, preserving fractional values. Added regression tests for positive and negative fractional readings.","diagnosis":"The live path used `float(int(float(text)))`; the intermediate `int` truncated `2.75` to `2`.","remaining_uncertainty":"None for the reported live-ingestion behavior. The replay path was intentionally left unchanged to preserve existing compatibility.","verification":"`python3 -m unittest discover -s tests -v` passed all 5 tests. Python compilation checks also passed."}