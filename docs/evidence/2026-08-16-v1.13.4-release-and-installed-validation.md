# v1.13.4 release and installed validation — 2026-08-16

## Identity and authoritative state

- B078 worker PR #156 merged as
  `a9f21ce67b3a261f4565eaf0b432c5990980ecee`.
- Release-preparation PR #157 merged as
  `e151d3b7a3db019cac5188b12466727b43f90ef2`.
- Exact merged-main test runs `31919205651` and `31919614516` passed before
  publication.
- Release workflow `31919668638` published tag `v1.13.4` from exact commit
  `e151d3b7a3db019cac5188b12466727b43f90ef2`.
- The final release-evidence diff passed all 21 maintained suites in 59.2
  seconds. These dated validation lines were appended afterward.
- Independent read-only audit verified release identity, installed claims,
  canonical B078 state, derived graph coherence, documentation boundaries, and
  the pending closeout gates without finding a blocker.

## Release evidence matrix

| Contract item | Required result | Observed result | Status |
|---|---|---|---|
| Immutable source | Annotated tag dereferences to the prepared merged-main commit | `v1.13.4` tag object names `e151d3b7...` | Passed |
| GitHub Release | Published, not draft or prerelease, with one reproducible MCP bundle | Release published at 2026-08-16T01:29:19Z; asset size 66,718 bytes | Passed |
| Bundle identity | Downloaded asset SHA-256 matches `server.json` | `7101ce40cee2b5023ef162d9d0cbd637edfdd2c94abd51ff2dd7cbe2509d1705` | Passed |
| MCP Registry | v1.13.4 is active/latest and names the same asset and checksum | Registry reports active, `isLatest: true`, exact URL and SHA-256 | Passed |
| PyPI | v1.13.4 wheel and source distribution are present and not yanked | Wheel 66,873 bytes; source 72,181 bytes; both unyanked | Passed |
| Product site | Public site responds and retains current product boundaries | HTTP 200, 33,350 bytes, 19-tool and approval markers present | Passed |

PyPI file SHA-256 values are
`a7759362f9fca966714ce4d542375bf7e28291bd8ef99b85cdf2a7837a3230dd`
for `engineering_board_mcp-1.13.4-py3-none-any.whl` and
`0cbcaf0e5f5e0009cc1bc52803c7a0318cdbb1801b3014c96bda6443ced26b70`
for `engineering_board_mcp-1.13.4.tar.gz`.

## Installed Codex validation

- Refreshed only the configured Engineering Board marketplace, then used the
  supported remove-and-add upgrade flow.
- Exactly one enabled `engineering-board@engineering-board` installation
  reports version 1.13.4, immutable ref `v1.13.4`, installation policy
  `AVAILABLE`, and authentication policy `ON_INSTALL`.
- The installed cache is
  `/home/rhenm/.codex/plugins/cache/engineering-board/engineering-board/1.13.4`.
  Its Git HEAD is exact release commit `e151d3b7...`.
- Codex registers exactly one Engineering Board MCP server from that v1.13.4
  cache. The packaged Codex validation test passes and exposes 19 tools.
- A fresh `codex exec` process completed installed read-only `board_status`
  without a per-call approval and returned canonical `B078 in_progress`.

## Installed Claude validation

- Added the released Engineering Board marketplace and installed
  `engineering-board@engineering-board` at user scope.
- A fresh Claude process lists exactly one enabled Engineering Board plugin at
  version 1.13.4. `claude plugin details` loads its skills, agents, hooks, and
  one MCP server from the installed cache.
- Normal and strict installed-path validation both exit 0 with
  `Validation passed` and no warning.
- Installed contract inspection reports Claude marketplace policy absent and
  Codex policy values `AVAILABLE` and `ON_INSTALL` present.

## Installed byte equivalence

Both installed host copies match the prepared and tagged source for all three
manifest boundaries:

| File | SHA-256 |
|---|---|
| `.claude-plugin/marketplace.json` | `7b502c1478377e2fdf229f85b054e62cac4b8eb1d75fdca6bba0bd6a706c03d9` |
| `.claude-plugin/plugin.json` | `8b44999f9da4747ed75d4e00800bd4f2e3b1cc5c310f3583a3832925eeca6817` |
| `.agents/plugins/marketplace.json` | `4afc7bed15f867a89075ee6a58f9ef92b35fb05979a839f1cc04933e85623b0d` |

## Cleanup

The release-host strict-validation copy under `D:\Codex\Scratch` was
fingerprinted before removal. Cleanup removed 637 files and 104 directories,
totaling 6,356,812 logical bytes. Measured free space increased by 7,426,048
bytes, no target failed, and the empty task root was removed. The installed
host caches and authoritative WSL repository were preserved.

## Documentation and evidence boundary

- Release preparation advanced both plugin manifests, both marketplaces,
  README, Architecture, the product-spec release boundary, MCP manifests, PyPI
  metadata, and changelog through the documented script.
- Security remained unchanged because v1.13.4 is within the supported 1.13.x
  minor and changes no effective authorization boundary.
- Setup, examples, visuals, MCP semantics, context, graph, hypothesis, outcome,
  and Learning behavior are unaffected. The worker PR already records that
  reviewed-unaffected rationale.
- This evidence proves publication identity, installed host schema behavior,
  and package registration. It does not prove that agents retrieve more useful
  memory or find better root causes. North-star product-effect evaluation
  remains separate.

## Remaining gate

B078 remains open until this release evidence merges, its exact merged-main
check passes, and the canonical entry is revalidated before resolution.
