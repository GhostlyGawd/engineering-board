# v1.13.5 release and installed validation — 2026-09-09

## Identity and authoritative state

- Dogfood product PR [#167](https://github.com/GhostlyGawd/engineering-board/pull/167)
  merged as `8f2bc716f1ea71c873ba26e31c0e679e15d7696a`.
- Release-note completion PR
  [#168](https://github.com/GhostlyGawd/engineering-board/pull/168)
  merged as `746de66`.
- Coordinated release-preparation PR
  [#169](https://github.com/GhostlyGawd/engineering-board/pull/169)
  merged as `a6207e6dffc061b92909cf5e17cdd4b25d1264e4`.
- Release workflow
  [`34311722593`](https://github.com/GhostlyGawd/engineering-board/actions/runs/34311722593)
  published `v1.13.5` from that exact main commit.
- The source and installed complete suites each passed all 21 maintained
  suites. The release-preparation PR's two `run-all` jobs also passed.

## Released changes

- The evaluation harness accepts the root-owned macOS `/var` and `/tmp`
  aliases while it continues to reject attacker-controlled symlink traversal
  below them.
- `board_patterns` returns a self-contained preview identifier for apply-only
  MCP follow-ups. Existing 64-character plan identifiers remain compatible
  when a caller repeats the original action and inputs.
- MCP regression coverage exercises apply-only behavior for both
  `board_patterns` and `board_hypotheses`.
- The completed Codex dogfood board and source-locked Q003 presentation
  diagnostic are preserved as repository evidence without a product-effect
  claim.

## Publication evidence

| Contract item | Required result | Observed result | Status |
|---|---|---|---|
| Immutable source | Annotated tag dereferences to the prepared merged-main commit | `v1.13.5^{}` resolves to `a6207e6dffc061b92909cf5e17cdd4b25d1264e4`; the tag object type is `tag` | Passed |
| GitHub Release | Published, not draft or prerelease, with one reproducible MCP bundle | Published at 2026-09-09T04:38:14Z; asset size 67,153 bytes | Passed |
| Bundle identity | Downloaded asset SHA-256 matches `server.json` | `96311c67f284fa7a48a50ffb10a73947e7e66355de4702a2b60b152101632d45` | Passed |
| MCP Registry | v1.13.5 is active/latest and names the same asset and checksum | Published at 2026-09-09T04:38:16.7034Z; `isLatest: true`; URL, version, and SHA-256 match | Passed |
| PyPI | v1.13.5 wheel and source distribution are present and not yanked | Wheel 67,311 bytes; source 72,565 bytes; both unyanked | Passed |
| Publication workflow | GitHub, registry, and PyPI jobs finish successfully against the release commit | Release job and PyPI job both completed successfully | Passed |
| Product site | GitHub Pages is built and public over enforced HTTPS | HTTP 200, 33,350 bytes; 19-tool and write-approval markers present | Passed |

The PyPI file SHA-256 values are
`63805d593e652ea2c868923ff3abdf4d8308101da99040cf3ba28e267ce795d2`
for `engineering_board_mcp-1.13.5-py3-none-any.whl` and
`a9f926d490efd5c1d1c6bb675371ec940b0d66d74f55e9c26b49e27644b932bf`
for `engineering_board_mcp-1.13.5.tar.gz`.

## Prepared-source validation

- `scripts/prepare-release.py 1.13.5` previewed and applied the same eleven
  coordinated version surfaces.
- The prepared bundle SHA-256 was
  `96311c67f284fa7a48a50ffb10a73947e7e66355de4702a2b60b152101632d45`.
- `bash tests/run-all.sh` passed 21 of 21 suites.
- `claude plugin validate --strict .` passed without a warning.
- Version coherence reported one aligned 1.13.5 value across the authoritative
  Claude manifest, Codex manifest, both marketplaces, MCP metadata, PyPI
  metadata, README, Architecture, and product-spec release boundary.
- Security remained on supported minor 1.13 because this is a compatible patch
  and does not change the effective authorization model.

## Installed Codex validation

- Only `engineering-board@engineering-board` is enabled from the Engineering
  Board marketplace at version 1.13.5 and immutable ref `v1.13.5`.
- The installed cache is
  `/Users/rhenmcleod/.codex/plugins/cache/engineering-board/engineering-board/1.13.5`.
- Its Git HEAD is exact release commit
  `a6207e6dffc061b92909cf5e17cdd4b25d1264e4`.
- Its packaged Codex validation exposes exactly 19 tools and passes.
- Its complete installed-cache suite passes 21 of 21 suites, including the 23
  evaluation tests on macOS.
- A fresh Codex process initialized a disposable board through the installed
  MCP server, previewed a P001 creation, then called `board_patterns` with only
  `root`, `project`, and the returned `apply` identifier. The receipt returned
  `applied: true`; a subsequent list returned P001 with the expected label.

## Installed Claude validation

- `engineering-board@engineering-board` is installed and enabled at user scope
  as version 1.13.5.
- The installed cache is
  `/Users/rhenmcleod/.claude/plugins/cache/engineering-board/engineering-board/1.13.5`.
- `claude plugin details` loads 26 skills, 8 agents, 4 hooks, and one
  Engineering Board MCP server from the installed release.
- Strict validation of the installed marketplace exits 0 with
  `Validation passed` and no warning.
- The installed Codex and Claude copies match the tagged source at the three
  host-manifest boundaries:

| File | SHA-256 |
|---|---|
| `.claude-plugin/plugin.json` | `40486b4f09f49bd0fbbebe3e4f745dc25b5a5086a481185529429589cc4c236a` |
| `.agents/plugins/marketplace.json` | `bfc128288317ae1461f7d8451e7b7c419bbaecfae257982d60f3c9b19e1e6d03` |
| `.claude-plugin/marketplace.json` | `e86d29e4b1a2323934bc2ba334d7a897c252ee26ade66aa7b0756d26e8911223` |

## Product and evidence boundary

- This release verifies package identity, host installation, MCP behavior,
  macOS compatibility, and publication alignment.
- Q003's four-arm diagnostic found zero strict cross-incident first causes
  across raw/prose and before/after presentation. It falsifies presentation
  alone as a sufficient C04 mechanism under that bounded diagnostic.
- Neither the Q003 result nor this release proves that repository context
  improves agent diagnoses. Version 4 remains unlocked, no scored baseline is
  authorized, and no product-effect claim is made.
- The next product investigation must change the memory-use contract or the
  information boundary, rather than rearrange the same context.
