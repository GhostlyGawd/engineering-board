# v1.13.2 release and installed-plugin validation — 2026-08-15

## Outcome

Engineering Board v1.13.2 shipped from merged `main`. The release separates
Codex from Claude-only automatic hooks, reports only canonical `## Open` rows
at Claude SessionStart, keeps ephemeral board state out of Git status, and
aligns current and historical product documentation.

This record proves repository, publication, installation, and installed MCP
behavior. It does not prove that Engineering Board improves agent diagnosis.
The latest D.1 comparative result remains below its 25-point acceptance gate.

```text
release: v1.13.2
commit: 2db7cd13fb1337e98e31e6fa3bf33390520ad995
release_workflow: 31864034389
bundle: engineering-board-mcp.mcpb
bundle_size: 65884 bytes
bundle_sha256: afd0b48b4c7c9ed2bef1b056f3908aeb5e9bdbecf4285e81f4415ad921fb3a9e
installed_codex_version: 1.13.2
installed_revision: 2db7cd13fb1337e98e31e6fa3bf33390520ad995
```

## Release and installation evidence

| Surface | Observation | Result |
|---|---|---|
| Pull request | PR #150 merged as `2db7cd13fb1337e98e31e6fa3bf33390520ad995`. Its push and pull-request test runs passed. | Passed |
| Merged main | Test run `31864002328`, Pages run `31864002317`, and dependency-graph run `31864004068` passed for the release commit. | Passed |
| Release workflow | Run `31864034389` created the tag and release, verified the bundle, published the official MCP Registry record, and published PyPI artifacts. | Passed |
| Immutable tag | Annotated tag `v1.13.2` dereferences to the merged release commit. | Aligned |
| GitHub Release | The release is neither a draft nor a prerelease. Its 65,884-byte MCPB download produced the pinned SHA-256 value above. | Published and aligned |
| PyPI | Version 1.13.2 exposes one 66,046-byte wheel and one 70,791-byte source distribution. Both files are unyanked. | Published |
| MCP Registry | The official record reports version 1.13.2 as active and latest. Its bundle URL and SHA-256 value match the GitHub Release. | Published and aligned |
| Product site | GitHub Pages returned a 33,243-byte response with the 19-tool product statement. | Available |
| WSL install | A marketplace refresh plus supported remove-and-add installed version 1.13.2 from Git ref `v1.13.2`. The installed Git HEAD and exact tag match the release commit. | Passed |
| Installed package | The installed Codex manifest selects `hooks/codex-hooks.json`, whose hook map is empty. The Claude hook source still declares PostToolUse, SessionStart, Stop, and UserPromptSubmit. Five board skills are installed. | Passed |

## Installed runtime evidence

A fresh Codex 0.145.0 process loaded the installed `engineering-board` MCP
server and attempted `board_status` and `board_context` calls. Its event stream
contained no Claude prompt-hook warning, no SessionStart root error, and no
Stop failure. This is the installed-host evidence for B071 and B072.

The non-interactive host then cancelled each MCP call while waiting for an
approval elicitation. The run therefore does not count as successful tool-call
evidence. A direct stdio session against the installed launcher supplied that
separate evidence:

- initialization reported Engineering Board version 1.13.2;
- `tools/list` returned 19 tools;
- `board_status` returned B071 through B076 as the six in-progress entries;
- two identical hook-boundary context requests returned `L002, L001`, context
  fingerprint `ctx-fc3925c3b11a8130`, and source fingerprint
  `ebbff5877839f89bdf2535a4b4accf97d68d5ea348509cd70a84a074a8e3ff24`;
- the complete repeated payloads were equal;
- the unrelated `assets/brand/tokens.css` control returned no result; and
- the launcher wrote no stderr output.

The same installed `board-session-start.sh` ran with the supported Claude
project-root environment. It reported exactly six rows, B071 through B076,
from `## Open`. It rendered no Conventions example. This is the installed
runtime evidence for B073.

## Lifecycle disposition

- B071 and B072 can resolve because the installed Codex manifest selected the
  empty Codex hook source and a fresh process showed none of the prior
  Claude-hook errors.
- B073 can resolve because the installed Claude SessionStart path rendered
  only the six canonical open rows.
- B074 can resolve because release claims and the B077 capture/promotion cycle
  created no visible `_claims/`, live `_sessions/`, migration snapshot, or
  derived-cache path in `git status`; canonical board files remained visible.
- B075 can resolve because the released current-behavior specification and
  deterministic coherence checks describe the shipped graph, context v3,
  ranking v2, outcomes, and failed D.1 gate.
- B076 can resolve because the three released root audits identify themselves
  as historical snapshots and point readers to current normative sources.

## New dogfood finding

The installed tool list contains no MCP read/write annotations. Separately,
fresh non-interactive Codex runs cancelled read-only board calls during an
approval elicitation. These observations do not establish that the missing
annotations caused the cancellation. B077 preserves the correlation and
requires an annotated-versus-unannotated control before selecting a fix. The
cancelled calls are not concealed by the successful direct launcher test, and
no dangerous approval bypass was used.

## Documentation alignment

The release changed README, Architecture, Product Evolution Spec, Security,
LLM guidance, changelog, release instructions, the maintainer release skill,
historical audit labels, and the pre-release evidence workpad. This closeout
adds dated release truth and board lifecycle changes only.

Canonical Markdown authority, graph and context contracts, H### and L###
record formats, renderer behavior, examples, visuals, permissions, privacy,
telemetry, hosted-service boundaries, and evaluation baselines were reviewed.
They are unaffected by this observation-only closeout. B077 will test its
causal hypothesis and perform its own behavior and documentation alignment
before any later release.

## Closeout validation

After lifecycle updates, a forced full graph build produced source fingerprint
`8cde9c7dfcd7302ca18e53449948abc56d0a687fc37d932ecc9deb0a15f7deb5`.
The open graph contains only B077. BOARD contains B077 plus the five active
Learning records, while ARCHIVE contains B071 through B076.

The settled closeout diff passed all 21 maintained suites in 56.5 seconds.
`git diff --check` passed. Normal `git status --short` contained no claim,
live-session, migration-snapshot, or derived-cache path.
