# v1.13.3 release and installed-plugin validation — 2026-08-16

## Outcome

Engineering Board v1.13.3 shipped from merged `main`. All 19 MCP tools now
publish explicit side-effect annotations. The Codex plugin uses the `writes`
approval mode by default. A fresh installed Codex process completed real
`board_status` and `board_context` calls without a per-call prompt, while a
write-capable control remained approval-gated.

This record proves repository, publication, installation, approval, and
installed runtime behavior. It does not prove that Engineering Board improves
agent diagnosis. The latest D.1 comparative result remains below its 25-point
acceptance gate.

```text
release: v1.13.3
commit: f4b44d9c8b63def225be2f3e9daa22309713768f
release_workflow: 31917453698
bundle: engineering-board-mcp.mcpb
bundle_size: 66718 bytes
bundle_sha256: 6098da585eabe6f781d0626caeee190523f107c8f4fa10ba4bbdc161e831153f
installed_codex_version: 1.13.3
installed_revision: f4b44d9c8b63def225be2f3e9daa22309713768f
codex_host_version: 0.145.0
```

## Release and installation evidence

| Surface | Observation | Result |
|---|---|---|
| Product pull request | PR #152 merged the B077 correction as `caec5539482fb8785d8f9831d4df43125f80a953`. Both pull-request test runs passed. | Passed |
| Release pull request | PR #153 merged the coordinated 1.13.3 preparation as the release commit above. Both pull-request test runs passed. | Passed |
| Merged main | Test run `31917396903` and dependency-graph run `31917399179` passed for the release commit. | Passed |
| Release workflow | Run `31917453698` created the tag and release, reproduced the bundle, and published the MCP Registry and PyPI records. | Passed |
| Immutable tag | Annotated tag `v1.13.3` dereferences to the release commit. | Aligned |
| GitHub Release | The release is neither a draft nor a prerelease. Its 66,718-byte MCPB download produced the pinned SHA-256 value above. | Published and aligned |
| PyPI | Version 1.13.3 exposes one 66,874-byte wheel and one 72,176-byte source distribution. Both files are unyanked, and the version-specific description contains the annotation and context-v3 changes. | Published |
| MCP Registry | The official record reports version 1.13.3 as active and latest. Its bundle URL and SHA-256 value match the GitHub Release. | Published and aligned |
| Product site | GitHub Pages returned HTTP 200 with a 33,350-byte response. It contains the 19-tool statement and the write-capable approval boundary. | Available |
| WSL install | A scoped marketplace refresh plus supported remove-and-add installed version 1.13.3 from Git ref `v1.13.3`. The installed Git HEAD equals the release commit. | Passed |
| Installed package | Exactly one Engineering Board plugin and one Engineering Board MCP registration are active. The Codex manifest selects `codex-mcp.json`; its approval mode is `writes`; the Codex hook map is empty; five board skills are installed. | Passed |

## Installed runtime evidence

The installed launcher initialized as version 1.13.3 and returned exactly 19
public tools. Every tool exposed the four Boolean annotation keys. The six
read-only tools were `board_context`, `board_get_entry`, `board_insights`,
`board_list_entries`, `board_list_projects`, and `board_status`. Two
`tools/list` calls were equal, no internal handler was exposed, and the
launcher wrote no stderr output.

A fresh Codex 0.145.0 process used the installed plugin configuration without
a hand-written MCP server override:

- `board_status` completed and reported B077 in progress, B078 ready, and no
  unpromoted scratch;
- `board_context` completed with context contract version 3, ranking rule
  version 2, context fingerprint `ctx-89f474dbf66afacc`, and results L005 and
  L003; and
- a valid `board_update_entry` call against nonexistent B999 reached the MCP
  call state and returned `user cancelled MCP tool call` before server
  execution.

No dangerous approval bypass or persistent policy override was used.

## Consecutive representative passes

Two later passes used separate temporary Git repositories and the installed
launcher. Each pass initialized a board, captured and promoted B001, created a
path-scoped L001 Learning, acquired and released the B001 claim, built the
full graph, retrieved L001 twice with identical context payloads, and ran the
installed Claude SessionStart hook from the repository root. SessionStart
reported the one canonical B001 row, surfaced L001 as systemic memory, emitted
no context warning, and rendered no Conventions example. Temporary
repositories were removed automatically.

| Pass | Graph fingerprint | Context fingerprint | Retrieved memory | Result |
|---|---|---|---|---|
| 1 | `c052d0765f82bd4a9eeae2666f57277bac54e778dfc9ae7ad4582286b970df46` | `ctx-34b691dfef1c23ef` | L001 | Passed |
| 2 | `dcee9cd0c152eba392da9acd7d14cdec04238fd18c8afb8c52289ca3ecfac205` | `ctx-c8386bb0ef97fa66` | L001 | Passed |

Earlier isolated controls returned an empty context result because they
contained only an unpatterned bug. They did not count as representative
retrieval passes. The first strengthened hook control also ran from the chat
directory while its project environment named the temporary repository. Its
context warning was a harness working-directory mismatch. The counted passes
ran from the repository root and required non-empty L001 retrieval with no
warning.

## Remaining dogfood finding

Normal Claude plugin validation passes with one warning because
`.claude-plugin/marketplace.json` contains an unsupported `plugins[0].policy`
field. Strict validation treats that warning as an error. B078 preserves this
separate host-schema defect. It did not cause the Codex approval failure and
was not concealed by the successful 1.13.3 controls.

## Documentation alignment

The product change updated README, Architecture, Product Evolution Spec,
Security, LLM guidance, landing copy, the MCP reference, changelog, and dated
B077 evidence. Release preparation advanced every coordinated version and
bundle surface. This closeout adds dated publication and installed-runtime
truth plus B077 lifecycle evidence.

Canonical Markdown authority, graph and context contracts, H### and L###
record formats, renderer behavior, examples, visuals, permissions, privacy,
telemetry, hosted-service boundaries, and evaluation baselines were reviewed.
They are unaffected by this evidence-only closeout. B078 remains open for its
own behavior, test, and documentation alignment.

## Closeout validation

The settled evidence diff passed all 21 maintained suites in 56.34 seconds.
`git diff --check` passed, and the recorded graph fingerprint matched current
canonical entries. Only this dated result was appended afterward.

The evidence pull request and exact merged-main checks remain pending. B077
must stay open until those checks pass and the canonical entry is revalidated.
