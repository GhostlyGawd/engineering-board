# D1-C08 evidence

One configuration example uses the removed `--legacy-output` flag. The current
replacement is `--output-format text`. No runtime configuration failure is
present in the supplied evidence.
