Extend the selected Common Thread direction into a pricing page, an email header, and three social sizes using the supplied written brand specification, repository tokens, logo rules, and local assets.

Deliver reusable source, every required render, and a token/component mapping. Do not redraw the logo. Preserve the exact legal line. When sources conflict, apply the precedence stated in `DESIGN.md`. Adapt the brand accessibly rather than using a permitted but unreadable color pairing. Use only supplied local assets and allowlisted packages, and retain complete evidence metadata.
