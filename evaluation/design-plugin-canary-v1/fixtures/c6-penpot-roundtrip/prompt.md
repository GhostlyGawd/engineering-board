Using the eligible editable-canvas path specified by the opaque arm, move the supplied pricing screen from code into an editable canvas. Change plan emphasis and mobile card order in the canvas, then apply only the approved edits back to the repository while preserving component semantics and unrelated code. Keep component instances linked, use a semantic token for the requested color, preserve logical DOM reading order even when visual mobile order differs, and prove the nearby unrelated section remains byte-identical. Deliver editable objects/components, before/after canvas renders, bounded diff, unchanged snapshot, final browser renders, and a reversible audit trail. Read-only context arms cannot attempt or score this task.
