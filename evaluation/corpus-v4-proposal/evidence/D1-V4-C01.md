# D1-V4-C01 evidence

Partner batch 73 leaves 18 orders in manual review. The validation log reports
`region rejected` for each affected order. Each displayed region value matches
a value on an accepted order. Reprocessing the unchanged batch gives the same
result.
